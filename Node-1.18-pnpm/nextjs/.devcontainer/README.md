- This should be run inside the repo directory

## How to Use:
1. Put the `.devcontainer` Inside the repository
    ```
     /my-project
        ├── node_modules
        ├── package.json
        └── /.devcontainer
            ├── devcontainer.json
            ├── compose.yaml
            └── Dockerfile
    ```
